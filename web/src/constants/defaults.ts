import type { GridSettings } from '@/types/design'

export const DEFAULT_GRID_SETTINGS: GridSettings = {
  widthFt: 50,
  heightFt: 50,
  increment: '1ft',
}

export const MAX_GRID_FT = 200
export const MAX_UNDO_DEPTH = 50
